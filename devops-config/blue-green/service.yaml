# 切換流量：把 selector 的 version 從 blue 改成 green，就把全部流量切到新版
apiVersion: v1
kind: Service
metadata:
  name: devops-demo
spec:
  type: NodePort
  selector:
    app: devops-demo
    version: blue          # ← 驗證 green 沒問題後，改成 green 即完成切換
  ports:
    - port: 8000
      targetPort: 8000
